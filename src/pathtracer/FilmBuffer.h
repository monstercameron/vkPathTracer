#pragma once

// Transitional focused include for film accumulation and resolve types.
// Declarations remain in PathTracer.h until the public pathtracer types are
// split from the umbrella header.
#include "pathtracer/PathTracer.h"
